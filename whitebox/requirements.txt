# Core — required for the base pipeline (explain, distill, attack, defend)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
click>=8.1

# Explainability — Whitebox falls back to a rough permutation-importance
# approximation if this isn't installed, but real SHAP is strongly
# recommended for any report you intend to trust.
shap>=0.44

# LLM narration layer (optional) — see whitebox/config.py for where to
# put your GROQ_API_KEY. Omit these two lines if you don't want the
# plain-English narration feature at all; the rest of the pipeline
# still runs fine without it.
groq>=0.9
python-dotenv>=1.0

# Only needed if your own model is a Keras/TensorFlow model — see
# examples/keras_adapter.py. Not required for scikit-learn/XGBoost/
# LightGBM models, or the built-in demo.
# tensorflow>=2.14
