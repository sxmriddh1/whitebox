# Copy this file to `.env` (same directory) and paste your real key below.
# `.env` is already listed in .gitignore — it will never be committed.
#
# Get a free key at: https://console.groq.com
#
# This is entirely optional. Whitebox's core audit (SHAP, surrogate tree,
# adversarial attacks, defenses) works with zero API key. Only the
# plain-English narration layer (Section 3 of the report) needs this.

GROQ_API_KEY=
